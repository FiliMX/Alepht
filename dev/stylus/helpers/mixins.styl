/*------------------------------------*\
    2. $Mixins
\*------------------------------------*/

// Mixin for buttons
button-style($border-color, $background, $color)
    border 1px solid $border-color
    background $background
    color $color
    
    &:active
        transform scale(0.9)